# Core: model, training, and the evaluation harness
torch>=2.1
torchvision>=0.16
numpy>=1.24
pillow>=10.0
scikit-image>=0.22          # PSNR / SSIM in eval_metrics.py
scipy>=1.11
lpips>=0.1.4                # optional perceptual loss (train.py --use_perceptual)
pytorch_msssim>=1.0.0

# Analysis and plotting (lam.py, benchmark.py, ab_test.py)
matplotlib>=3.8
pandas>=2.0
opencv-python>=4.8
numba>=0.59

# Live screen-overlay demo only (overlay.py, app_overlay.py) -- not needed for
# training, evaluation, or inference. Install with:  pip install -r requirements-demo.txt
